filters:
  type: passport
entities:
  entity:
    schema: Person
    key: entity_id
    properties:
      passportNumber:
        column: number
      nationality:
        column: country_code
      # summary:
      #   column: summary
